{
  "name": "prisma-practice-bugtracker",
  "version": "1.0.0",
  "type": "module",
  "description": "Practice: model a small Bug Tracker and run CRUD with Prisma.",
  "scripts": {
    "migrate": "prisma migrate dev",
    "generate": "prisma generate",
    "studio": "prisma studio",
    "queries": "node queries.js"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3"
  },
  "devDependencies": {
    "prisma": "^6.19.3"
  }
}
